#include <iostream>
#include "vector.hpp"
#include <vector>

int main()
{
	std::vector<int> test;
	std::vector<int>::iterator it = test.rbegin();

	if (it > it2)
	{
		return (1);
	}
	std::vector<int> test2;
	test2 >= test;
}